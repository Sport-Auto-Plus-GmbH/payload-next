# Generated by Payload's own CLI — reformatting would just be undone on the next
# generate:types / generate:importmap / migrate:create run.
src/payload-types.ts
src/app/(payload)/admin/importMap.js
src/migrations/**

# Prose docs, not code — hand-wrapped, not meant to be machine-reformatted.
.ai/**

pnpm-lock.yaml
